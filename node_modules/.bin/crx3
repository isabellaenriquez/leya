#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../crx3/bin/crx3.js" "$@"
  ret=$?
else 
  node  "$basedir/../crx3/bin/crx3.js" "$@"
  ret=$?
fi
exit $ret
